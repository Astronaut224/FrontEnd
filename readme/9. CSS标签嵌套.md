# 1. CSS标签嵌套

块标签可以嵌套其他块标签、行内块标签和行内标签。



特殊情况：p标签不能嵌套其他块标签。

```html
<p>
	<div>查看f12</div>
</p>
```

检查浏览器解析代码后发现p标签并没有嵌套div标签：

```html
<p></p>
<div>查看f12</div>
<p></p>
```



a标签可以嵌套任意标签，除了a标签自己。

```html
<a href="#"><div>hhhhhhhhh</div></a>
```

